//Program to check ia a string ia a palindrome using recusrion
#include<stdio.h>
#include<string.h>
int ispalindrome(char str[],int index,int len){}
    if 
}

int main(){
    char str[10];
    printf("Enter the string: ");
    gets(str);
    int n=ispalindrome(str,0,strelen(str))
    if(n){
        printf("String is Palindrome!");
    }
    else{
        printf("String is not Palindrome!");
    }
}